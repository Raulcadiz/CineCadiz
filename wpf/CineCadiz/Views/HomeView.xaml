<UserControl x:Class="CineCadiz.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:models="clr-namespace:CineCadiz.Models"
             Background="{StaticResource AppBackground}">

    <UserControl.Resources>
        <!-- Card hover animation -->
        <Style x:Key="MediaCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                 To="1.04" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                 To="1.04" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                 To="1" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                 To="1" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Loading overlay -->
        <Border Background="#88000000" Panel.ZIndex="10"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Foreground="{StaticResource AccentRed}" />
                <TextBlock Text="Cargando..." Foreground="#AAA" FontSize="13"
                           HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Error banner -->
                <Border Background="#C62828" Padding="16,12" Margin="24,16,24,0"
                        CornerRadius="8"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="WifiOff" Foreground="White" Width="20" Height="20"
                                                 VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="White"
                                   VerticalAlignment="Center" FontSize="13"/>
                    </StackPanel>
                </Border>

                <!-- ══ Continuar viendo ══ -->
                <StackPanel Visibility="{Binding HasContinueWatching,
                            Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="24,24,24,0">

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                        <Border Width="4" Height="22" Background="{StaticResource AccentRed}"
                                CornerRadius="2" Margin="0,0,10,0"/>
                        <TextBlock Text="Continuar viendo" FontSize="18" FontWeight="Bold"
                                   Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding ContinueWatching}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:ProgressEntry}">
                                    <Border Style="{StaticResource MediaCard}"
                                            Width="148" Height="210" Margin="0,0,14,4"
                                            MouseLeftButtonUp="ContinueCard_Click" Tag="{Binding}">
                                        <Grid ClipToBounds="True">
                                            <Image Source="{Binding Image, Converter={StaticResource ImageUrlConverter}}"
                                                   Stretch="UniformToFill"/>
                                            <!-- Gradient overlay -->
                                            <Border>
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Color="Transparent" Offset="0.5"/>
                                                        <GradientStop Color="#EE000000" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                            </Border>
                                            <!-- Progress bar -->
                                            <ProgressBar Value="{Binding ProgressFraction, Mode=OneWay}"
                                                         Maximum="1" Height="3"
                                                         VerticalAlignment="Bottom"
                                                         Background="#33FFFFFF"
                                                         Foreground="{StaticResource AccentRed}"
                                                         BorderThickness="0"/>
                                            <!-- Info -->
                                            <StackPanel VerticalAlignment="Bottom" Margin="10,0,10,10">
                                                <TextBlock Text="{Binding Title}" Foreground="White"
                                                           FontSize="12" FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding EpisodeLabel}" Foreground="#CCC"
                                                           FontSize="10" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>

                <!-- ══ Novedades ══ -->
                <StackPanel Margin="24,28,24,24">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                        <Border Width="4" Height="22" Background="{StaticResource AccentRed}"
                                CornerRadius="2" Margin="0,0,10,0"/>
                        <TextBlock Text="Novedades" FontSize="18" FontWeight="Bold"
                                   Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding Trending}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:ContentItem}">
                                <Border Style="{StaticResource MediaCard}"
                                        Width="164" Height="252" Margin="0,0,14,14"
                                        MouseLeftButtonUp="TrendingCard_Click" Tag="{Binding}">
                                    <Grid ClipToBounds="True">
                                        <Image Source="{Binding Image, Converter={StaticResource ImageUrlConverter}}"
                                               Stretch="UniformToFill"/>
                                        <!-- Gradient overlay -->
                                        <Border>
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="Transparent" Offset="0.45"/>
                                                    <GradientStop Color="#F0000000" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        <!-- Type badge -->
                                        <Border HorizontalAlignment="Left" VerticalAlignment="Top"
                                                Margin="8" Background="#CC000000" CornerRadius="4" Padding="6,3">
                                            <TextBlock Text="{Binding Type}" Foreground="#E53935"
                                                       FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        <!-- Info -->
                                        <StackPanel VerticalAlignment="Bottom" Margin="10,0,10,12">
                                            <TextBlock Text="{Binding Title}" Foreground="White"
                                                       FontSize="13" FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextWrapping="NoWrap"/>
                                            <TextBlock Text="{Binding YearDisplay}" Foreground="#AAA"
                                                       FontSize="11" Margin="0,3,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
