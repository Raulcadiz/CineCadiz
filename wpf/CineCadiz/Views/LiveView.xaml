<UserControl x:Class="CineCadiz.Views.LiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:models="clr-namespace:CineCadiz.Models"
             Background="{StaticResource AppBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Loading bar -->
        <ProgressBar Grid.Row="0" IsIndeterminate="True" Height="3" BorderThickness="0"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                     Foreground="{StaticResource AccentRed}"/>

        <!-- List selector (hidden when only one list) -->
        <Border Grid.Row="1" Background="#0D0D0D" Padding="20,8"
                Visibility="{Binding Lists.Count, Converter={StaticResource StringNotEmptyConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Lista:" Foreground="#666" FontSize="11"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ListBox ItemsSource="{Binding Lists}"
                         SelectedItem="{Binding SelectedList}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,0,6,0"/>
                            <Setter Property="Padding" Value="12,5"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="14"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="#1E1E1E"/>
                            <Setter Property="BorderBrush" Value="#2A2A2A"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentRed}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource AccentRed}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2A2A2A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:LiveListInfo}">
                            <TextBlock Text="{Binding Nombre}" FontSize="11"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Category chips -->
        <Border Grid.Row="2" Background="#0A0A0A" Padding="20,10">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Margin="0,0,8,0" Padding="14,7"
                                    Command="{Binding DataContext.SelectCategoryCommand,
                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#1E1E1E"/>
                                        <Setter Property="Foreground" Value="#AAA"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="#333"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="20" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource AccentRed}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentRed}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <TextBlock Text="{Binding}" FontSize="12"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Channels list -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Padding="20,12,20,16">
            <ItemsControl ItemsSource="{Binding Channels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:LiveGroup}">
                        <Border Background="{StaticResource CardBackground}"
                                CornerRadius="10" Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonUp="Channel_Click" Tag="{Binding}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#252525"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid Margin="14,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="52"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="36"/>
                                </Grid.ColumnDefinitions>

                                <!-- Logo -->
                                <Border Grid.Column="0" CornerRadius="8" ClipToBounds="True"
                                        Width="48" Height="48" Background="#2A2A2A">
                                    <Image Source="{Binding Image, Converter={StaticResource ImageUrlConverter}}"
                                           Stretch="Uniform"/>
                                </Border>

                                <!-- Name + category -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="14,0,10,0">
                                    <TextBlock Text="{Binding Title}" Foreground="White"
                                               FontSize="14" FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding GroupTitle}" Foreground="#777"
                                               FontSize="11" Margin="0,3,0,0"/>
                                </StackPanel>

                                <!-- Signals badge -->
                                <Border Grid.Column="2" CornerRadius="12" Background="#1A1A1A"
                                        Padding="10,4" VerticalAlignment="Center"
                                        Visibility="{Binding ChannelCount,
                                            Converter={StaticResource StringNotEmptyConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="SignalVariant" Width="12" Height="12"
                                                                  Foreground="#4CAF50" VerticalAlignment="Center"
                                                                  Margin="0,0,5,0"/>
                                        <TextBlock Foreground="#AAA" FontSize="11">
                                            <Run Text="{Binding ChannelCount}"/>
                                            <Run Text=" HD"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>

                                <!-- Play -->
                                <materialDesign:PackIcon Grid.Column="3" Kind="PlayCircle"
                                                          Width="28" Height="28"
                                                          Foreground="{StaticResource AccentRed}"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
